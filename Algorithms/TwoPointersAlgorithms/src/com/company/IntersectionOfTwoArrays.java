package com.company;

public class IntersectionOfTwoArrays {
    public int[] intersect(int[] nums1 , int[] nums2){

        // first get the length of each array use the longest
        int num1Length = nums1.length-1;
        int num2Length = nums2.length-1;

        // loop through every element in the longest array
            // for each of those elements compare them to
            // the elements in the second array if they're
            // equal put the array in the arraylist

        // loop through the arraylist and put every element
        // into the array

        return new int[]{};
    }
}
